<!--
     we're setting index to false because we don't want the entries folder to
     be indexable (indexable components are iterable when traversing components).
     Instead we're referencing it by the name "postEntries" from ../index.svelte. 
    -->

<!-- routify:options redirect="/posts" -->
<!-- routify:options index=false -->
<!-- routify:options name="postEntries" -->
