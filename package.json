{
  "name": "routify-templates",
  "version": "0.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "dev": "canvasit --watch -output output",
    "help": "canvasit help",
    "generate": "canvasit -output output",
    "test:output:dev": "cd output && npm run test:dev",
    "test:output:build": "cd output && npm run test:build",
    "build": "node generate"
  },
  "author": "",
  "license": "ISC",
  "canvasit": {
    "basepath": "fragments",
    "@exec": "npm install && npm dev",
    "ignore": [
      "node_modules",
      "dist",
      ".routify"
    ],
    "include": [
      "base"
    ]
  },
  "devDependencies": {
    "prettier-plugin-svelte": "^2.1.0",
    "svelte": "^3.31.2"
  },
  "dependencies": {
    "canvasit": "^0.1.2"
  }
}
